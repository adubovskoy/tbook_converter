# Go build output
/convert

# Secrets and resumable translation cache
.env
.tbook_cache/
lexicons/

# Local embedding-aligner venv (tools/embalign-setup.sh)
.venv-embalign/

# Books are test inputs, not sources
*.epub
*.fb2
*.tbook

# Run artifacts (reports, request metrics, experiment caches, lexeval binary)
/lexeval
.cache-*/
*.flagged.json
*.lexflagged.json
*.unverified.json
*stats.jsonl

# Python bytecode
__pycache__/

# Research arms: throwaway convert binaries and produced books (see issue #8)
/bench-quality/convert-*

# Research evidence: judge batches/keys/verdicts, probe sets, diffs, mined
# sentence dumps. Kept on disk (the reports cite it and /tmp is wiped between
# sessions) but never published: it is verbatim copyrighted book text.
/bench-quality/.artifacts/

# Go build output (GUI binary built straight into the repo root)
/convert-gui
